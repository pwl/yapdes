/**
 * \mainpage The frontpage of yapdes
 *
 * \section cc Coding conventions
 *
 * Return values are chosen to be 0 in case of success and >0 in case
 * of error. If passing a return value is not necessary the return
 * value should be void and any information about errors should be
 * logged or printed without returning an error code. However,
 * functions that test some property (e.g. ODE_trigger_test()) should
 * return either TRUE or FALSE (predefined in @ref common.h).
 */
